# Nixpacks configuration for Railway deployment
# Note: This project uses Dockerfile instead (see railway.json)

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn backend.server:app --host 0.0.0.0 --port ${PORT:-8000}"
